package uavforge.uavforge;

import android.app.Activity;
import android.os.Bundle;
import android.view.View;


public class MapPlanningTable extends Activity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_map_planning_table);
    }

    // Adds a new entry point from the table
    private void plusButtonClicked(View view) {

    }

    // Removes an entry point from the table
    private void removeButtonClicked(View view) {

    }
}
